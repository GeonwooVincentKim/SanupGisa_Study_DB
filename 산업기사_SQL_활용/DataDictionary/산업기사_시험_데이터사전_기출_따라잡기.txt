1. 데이터 사전 (Data Dictionary)

2. 메타 데이터

3.
- 1. USE
- 2. VIEWS


CREATE TABLE Instructor
id CHAR(5) PRIMARY KEY
name CHAR(15) NOT NULL,
dept CHAR(15)
FOREIGN KEY dept REFERENCES Department(dept)
ON DELETE SET NULL,
ON UPDATE CASCADE


ALTER TABLE patient ADD job CHAR(20)

CREATE VIEW CC(ccid, ccname, instname)
AS SELECT id, name, Instructor.name
FROM Course, Instructor
WHERE Course.id = Instructor.id


CREATE UNIQUE INDEX Stud_idx
ON Student(ssn)


CREATE DOMAIN 직위 VARCHAR2(10)
DEFAULT '사원'
CONSTRAINT VALID-직위 CHECK('사원', '대리', '과장', '부장', '이사', '사장')

