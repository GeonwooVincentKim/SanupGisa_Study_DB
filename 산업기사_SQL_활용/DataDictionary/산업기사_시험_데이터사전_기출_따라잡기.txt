1. 데이터 사전 (Data Dictionary)

2. 메타 데이터

3.
- 1. USE
- 2. VIEWS


SELECT 학과, COUNT(*) AS 학과별튜플수 FROM 학생 GROUP BY 학과 

SELECT 과목이름, MIN(점수) AS 최소점수, MAX(점수) AS 최대점수 FROM 성적 GROUP BY 과목이름 HAVING AVG(점수) >= 90

SELECT 이름, 전공, 신청과목 FROM 결제, 학생정보, 신청정보 WHERE 학생정보.학번 = 신청정보.학번 AND 신청과목 = "Java" GROUP BY 이름, 전공, 신청과목 HAVING = "컴퓨터공학"

200, 3000

SELECT SUM(psale) FROM Sale WHERE ID IN (SELECT name FROM Shop WHERE name LIKE = 'USB%')

SELECT 소속도시, AVG(매출액) FROM 지점정보 WHERE 매출액 > 1000 GROUP BY 소속도시 HAVING COUNT(*) >= 3

SELECT 장학내역, 장학금 ROW_NUMBER() OVER (PARTITION BY 장학내역 ORDER BY 장학금 DESC) AS NUM FROM 장학금

SELECT 학과, 장학내역 SUM(장학금) AS 장학금합계 FROM 장학금 GROUP BY ROLLUP(학과, 장학내역)
